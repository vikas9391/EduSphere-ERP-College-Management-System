import React from 'react';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing } from '../theme';
export function ProfileScreen(){return <ScrollView style={s.page} contentContainerStyle={s.content}><Text style={s.title}>My Profile</Text><Text style={s.muted}>Your authenticated Student profile will be displayed here using the same backend identity model as the web portal.</Text><View style={s.card}><Text style={s.cardTitle}>Account</Text><Text style={s.muted}>User identity is resolved through the Student profile; User.id is not assumed to equal Student.id.</Text></View></ScrollView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:colors.background},content:{padding:spacing.lg,paddingTop:spacing.xl},title:{fontSize:28,fontWeight:'800',color:colors.text},muted:{fontSize:15,lineHeight:23,color:colors.muted,marginTop:spacing.sm},card:{backgroundColor:colors.card,borderRadius:radius.card,padding:spacing.lg,marginTop:spacing.xl},cardTitle:{fontSize:18,fontWeight:'800',color:colors.text}});
